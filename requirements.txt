# Runtime dependencies for the offline agent.
#
#   pip install -r requirements.txt
#
# Mirrors [project].dependencies in pyproject.toml for the default runtime
# path. This file intentionally does NOT install llama-cpp-python; the default
# configuration uses a remote OpenAI-compatible server, and the local backend is
# optional.

# --- Agent core (lightweight; no GPU deps) --------------------------------
agent-client-protocol>=0.9,<0.11
mcp>=1.0
openai>=1.30
pydantic>=2
pydantic-settings>=2.2
