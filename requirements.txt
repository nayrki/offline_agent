# Runtime dependencies for the offline agent.
#
#   pip install -r requirements.txt
#
# Mirrors [project].dependencies in pyproject.toml and adds the local
# llama-cpp-python backend built against CUDA. For a fully offline / air-gapped
# build from the vendored sdist in deps/, use ./install.sh --offline (or the
# packaged `offline-agent-install`) instead of this file.

# --- Agent core (lightweight; no GPU deps) --------------------------------
agent-client-protocol>=0.10,<0.11
mcp>=1.0
openai>=1.30
pydantic>=2
pydantic-settings>=2.2

# --- Local model runtime (CUDA build from source) -------------------------
# Compiles llama-cpp-python with CUDA offload enabled. Requires the CUDA
# toolkit (nvcc) and a C/C++ compiler on PATH. For a CPU-only build, drop the
# config-settings flag (or run ./install.sh --cpu).
#llama-cpp-python -C cmake.args="-DGGML_CUDA=on"
